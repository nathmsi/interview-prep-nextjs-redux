/**
 * EXERCISE nodejs — Strip HTML Tags
 *
 * Remove <tag> segments from string.
 *
 * Hint: regex /<[^>]+>/g
 * Run: npm run node:12
 */

export function stripHtmlTags(html: string): string {
  // TODO: your code here
  return html;
}