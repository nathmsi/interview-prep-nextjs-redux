/**
 * EXERCISE algo/08 — Deep Clone
 *
 * Deep clone plain objects and arrays (no functions, Dates, or circular refs).
 * Primitives are returned as-is.
 *
 * Examples
 * - deepClone({ a: [1, { b: 2 }] }) → independent copy
 * - deepClone(5) → 5
 *
 * Hint: recursive — Array.isArray, spread or Object.fromEntries
 * Run: npm run algo:44
 */

export function deepClone<T>(value: T): T {
  // TODO: your code here
  throw new Error("Not implemented");
}
