/**
 * EXERCISE algo/07 — House Robber
 *
 * Rob non-adjacent houses for maximum loot. nums[i] = money in house i.
 *
 * Examples
 * - [1, 2, 3, 1]     → 4  (rob house 0 and 2)
 * - [2, 7, 9, 3, 1]  → 12 (rob house 0, 2, 4)
 * - [5]              → 5
 *
 * Hint: dp[i] = max(dp[i-1], dp[i-2] + nums[i])
 * Run: npm run algo:25
 */

export function rob(nums: number[]): number {
  // TODO: your code here
  return 0;
}
