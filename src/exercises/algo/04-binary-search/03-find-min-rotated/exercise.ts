/**
 * EXERCISE algo/04 — Find Minimum in Rotated Sorted Array
 *
 * A sorted array was rotated at an unknown pivot.
 * Return the minimum element. No duplicates.
 *
 * Examples
 * - [3, 4, 5, 1, 2]       → 1
 * - [4, 5, 6, 7, 0, 1, 2] → 0
 * - [11, 13, 15, 17]      → 11  (not rotated)
 *
 * Hint: binary search — compare mid with right; one half is always sorted
 * Run: npm run algo:15
 */

export function findMinRotated(nums: number[]): number {
  // TODO: your code here
  return 0;
}
