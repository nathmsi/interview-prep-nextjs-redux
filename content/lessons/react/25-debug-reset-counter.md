# React — Debug: Reset Counter

> **Exercise:** `content/exercises/react/08-debug-reset-counter/`  
> **Run:** `npm run react:08`

## Problem

When `resetKey` prop changes, counter must go back to **0**.

## Hint

Add a `useEffect` that runs when `resetKey` changes and calls `setCount(0)`.

## Interview angle

Syncing state to props — when to reset vs derive.
