/**
 * EXERCISE nodejs — Sanitize User Input
 *
 * Trim, lowercase email, cap name at 100 chars.
 *
 * Hint: Return new object
 * Run: npm run node:11
 */

export type UserInput = { email: string; name: string };
export function sanitizeUserInput(input: UserInput): UserInput {
  // TODO: your code here
  return input;
}