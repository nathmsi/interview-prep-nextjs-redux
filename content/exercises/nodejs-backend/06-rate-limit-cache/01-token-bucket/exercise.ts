/**
 * EXERCISE nodejs — Token Bucket Limiter
 *
 * Refill tokens over time. tryConsume returns false when empty.
 *
 * Hint: Track last refill timestamp
 * Run: npm run node:21
 */

export class TokenBucket {
  constructor(capacity: number, refillPerMs: number) {}
  tryConsume(): boolean { return false; }
} {
  // TODO: your code here
}