/**
 * EXERCISE nodejs — Parse Headers
 *
 * Parse `Key: value` lines. Lowercase keys. Trim values.
 *
 * Hint: Split on newlines, first colon separates key/value.
 * Run: npm run node:04
 */

export function parseHeaders(raw: string): Record<string, string> {
  // TODO: your code here
  return {};
}