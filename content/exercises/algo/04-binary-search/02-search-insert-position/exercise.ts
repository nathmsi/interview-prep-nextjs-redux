/**
 * EXERCISE algo/04 — Search Insert Position
 *
 * Given a sorted array and a target, return the index where target
 * would be inserted to keep the array sorted.
 *
 * Examples
 * - nums = [1, 3, 5, 6], target = 5  → 2
 * - nums = [1, 3, 5, 6], target = 2  → 1
 * - nums = [1, 3, 5, 6], target = 7  → 4
 * - nums = [1, 3, 5, 6], target = 0  → 0
 *
 * Hint: binary search — when not found, left pointer is the insert index
 * Run: npm run algo:14
 */

export function searchInsert(nums: number[], target: number): number {
  // TODO: your code here
  return 0;
}
